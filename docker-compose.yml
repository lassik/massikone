version: "2"
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/massikone/data
    env_file:
      - .env
    environment:
      DATABASE_URL: "sqlite:///massikone/data/massikone.db"

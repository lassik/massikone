<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/static/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/static/css/bootstrap-theme.min.css" />
    <title>{{AppTitle}}</title>
  </head>
  <body>
    <div class="container">
      <h1>{{AppTitle}}</h1>
      <h2>{{CurrentUser.FullName}}</h2>
      <div class="btn-group">
        <a class="btn btn-lg btn-warning" href="/">Peruuta</a>
      </div>
      <h2>Yhdistyksen tiedot</h2>
      <form enctype="multipart/form-data" method="POST" action="/api/settings">
        <table class="table table-striped table-hover">
          <tr>
            <th><label for="OrgFullName">Koko nimi:</label></th>
            <td>
              <input type="text" class="form-control"
                     name="OrgFullName" id="OrgFullName"
                     value="{{Settings.OrgFullName}}" />
            </td>
          </tr>
          <tr>
            <th><label for="OrgShortName">Lyhennetty nimi:</label></th>
            <td>
              <input type="text" class="form-control"
                     name="OrgShortName" id="OrgShortName"
                     value="{{Settings.OrgShortName}}" />
            </td>
          </tr>
        </table>
        <input type="submit" class="btn btn-lg btn-success" value="Tallenna" />
      </form>
      <h2>Käyttäjien oikeudet</h2>
      <form enctype="multipart/form-data" method="POST" action="/api/permissions">
        <table class="table table-striped table-hover">
          <tr>
            <th>Käyttäjä</th>
            <th>Oikeudet tositteisiin</th>
          </tr>
          {{#Users}}
            <tr>
              <td>{{FullName}}</td>
              <td>
                <select name="{{UserId}}">
                  <option value="0">Vain omat</option>
                  <option value="1">Selata kaikkea</option>
                  <option value="2">Muokata kaikkea</option>
                </select>
              </td>
            </tr>
          {{/Users}}
        </table>
      </form>
    </div>
    <script src="/static/js/jquery.min.js"></script>
    <script src="/static/js/bootstrap.min.js"></script>
  </body>
</html>
